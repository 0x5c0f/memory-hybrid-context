# 基线归档（2026-03-03）

本文档记录当前环境下 `memory-hybrid-context` 的已归档基线状态，用于后续回归比对。
注意：本文件中的“当前运行基线”是当时环境的实测快照，不等于插件代码默认值。

## 1. 归档时间

1. 日期：`2026-03-03`
2. 归档目录：
   - `./`

## 2. 当前运行基线

1. 主记忆插件：
   - `memory-hybrid-context`

2. 当前向量后端：
   - `ann-local`

3. 当前向量模式：
   - `ann-local-lsh`

4. 当前建议：
   - `probePerBand = 1`
   - `ann-health.tuning.action = keep`

## 3. 一致性基线摘要

来自：

1. [consistency-report.json](./consistency-report.json)
2. [consistency-report.md](./consistency-report.md)

当前摘要：

1. `auditedRecords = 29`
2. `recordIssues = 0`
3. `affectedRecords = 0`
4. `orphanIndexes = 0`
5. `missingRecordJobs = 0`
6. `staleRunningJobs = 0`
7. `failedJobs = 0`
8. `archiveIssues = 0`
9. `quarantinedFiles = 1`
10. `annHealthLevel = warn`

这说明：

1. 主记录与索引当前一致
2. 没有孤儿索引
3. 队列状态干净
4. 归档链路当前无异常
5. `ann-local` 仍有轻度热点桶告警，但这是性能观察项，不是故障

## 4. 归档治理基线摘要

来自：

1. [archive-report.json](./archive-report.json)
2. [archive-report.md](./archive-report.md)

当前摘要：

1. `linkedRecordsAudited = 29`
2. `linkedIssues = 0`
3. `orphanedFiles = 0`
4. `quarantinedFiles = 1`
5. `annHealthLevel = warn`

这说明：

1. 记录引用的归档文件都存在
2. 主归档区无孤儿文件
3. 隔离区中仍保留 `1` 个文件，需按治理策略人工决定恢复或清理

## 5. 本次归档文件

1. [consistency-report.json](./consistency-report.json)
2. [consistency-report.md](./consistency-report.md)
3. [archive-report.json](./archive-report.json)
4. [archive-report.md](./archive-report.md)

## 6. 后续使用建议

1. 每次大版本调整后，重新执行：
```bash
openclaw mhm consistency-report --limit 50 --format markdown
openclaw mhm archive-report --limit 50 --format markdown
```

2. 若一致性报告出现非零问题，先执行：
```bash
openclaw mhm consistency-repair --limit 50 --dry-run
```

3. 若 `annHealthLevel` 从 `warn` 继续恶化，优先观察：
```bash
openclaw mhm ann-health
openclaw mhm ann-stats
```
